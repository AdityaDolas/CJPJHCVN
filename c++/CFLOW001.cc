#include <iostream>
using namespace std;

int main()
{
    int n, a, b, r;
    cin >> n;
    while (n > 0)
    {
        cin >> a;
        cin >> b;
        ;
        r = a + b;
        cout << r << endl;
        n--;
    }
}